en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      # html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: "Please review the problems below:"
    titles:
      users:
        invite_email: Enter a valid email address
      survey:
        position_content: Use the % option to shift the survey relative to the browser width. Use the px to shift the survey a fixed number of pixels.
        inline_target_selector: Enter the id of a div
        mobile_inline_target_selector: Enter the id of a div
        sdk_inline_target_selector: Enter the name of the identifier
        top_position: The top offset of the widget
        bottom_position: The bottom offset of the widget
        right_position: The right offset of the widget
        left_position: The left offset of the widget
        sdk_widget_height: The height of the widget as a percentage (SDK only)
        width: Enter the width, in pixels
        background_color: Enter a hex value for background color, or select one with the color picker
        text_color: Enter a hex value for font color, or select one with the color picker
        answer_text_color: Enter a hex value for font color, or select one with the color picker
        fullscreen_margin: Margin of widget in percentage %
        triggers:
          trigger_content: Enter the URL of a page or a View name to include in the survey
        suppressers:
          trigger_content: Enter the URL of a page or a View name to exclude from the survey
    placeholders:
      survey:
        inline_target_selector: Enter the id of a div
        mobile_inline_target_selector: Enter the id of a div
        sdk_inline_target_selector: Enter the name of the identifier
