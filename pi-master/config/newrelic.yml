#
# This file configures the New Relic Agent.  New Relic monitors Ruby, Java,
# .NET, PHP, Python, Node, and Go applications with deep visibility and low
# overhead.  For more information, visit www.newrelic.com.
#
# Generated March 01, 2019
#
# This configuration file is custom generated for Pulse Insights_2
#
# For full documentation of agent configuration options, please refer to
# https://docs.newrelic.com/docs/agents/ruby-agent/installation-configuration/ruby-agent-configuration

common: &default_settings
  # Required license key associated with your New Relic account.
  license_key: <%= Rails.application.credentials.newrelic[:license_key] %>

  # To disable the agent regardless of other settings, uncomment the following:
  # agent_enabled: false

  # Logging level for log/newrelic_agent.log
  log_level: info


# Environment-specific settings are in this section.
# RAILS_ENV or RACK_ENV (as appropriate) is used to determine the environment.
# If your application has other named environments, configure them here.
development:
  monitor_mode: false

test:
  monitor_mode: false

develop:
  <<: *default_settings
  app_name: Pulse Insights_2 (Develop)

staging:
  <<: *default_settings
  app_name: Pulse Insights_2 (Staging)

production:
  <<: *default_settings
  app_name: Pulse Insights_2
