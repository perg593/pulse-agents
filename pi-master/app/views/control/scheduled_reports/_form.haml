= react_component("ScheduledReportForm", @presenter.form_attributes.merge(authenticityToken: form_authenticity_token))

- if current_user.admin? && @audits.present?
  = render "application/audits", audits: @audits
