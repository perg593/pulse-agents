:javascript
  if (typeof(window['pi']) != 'function') {
    window['pi'] = function() {
      window['pi'].commands = (window['pi'].commands || []);
      window['pi'].commands.push(arguments);
    }
  }
  window['pi'].commands = (window['pi'].commands || []);

  pi('debug', true);
  pi('host', '#{Rails.configuration.survey_host.html_safe}');
  pi('identify', '#{h(current_user.account.identifier)}');
  pi('preview', true);
  pi('spa', false);
