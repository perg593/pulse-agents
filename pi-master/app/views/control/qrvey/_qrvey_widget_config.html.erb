<%= content_tag :div, '', id: 'qrvey_widget_config', style: 'display: none;',
                data: {
                  domain: QRVEY_CONFIG[:domain],
                  qv_token: @qv_token,
                  dashboard_id: @dashboard_id,
                  survey_id: @survey&.id,
                  datasets: @datasets
                }
%>

<%= javascript_include_tag 'control/qrvey_widget_config' %>
